#include <bits/stdc++.h>
using namespace std;

int main() {
	ios::sync_with_stdio(false), cin.tie(nullptr), cout.precision(15), cout.setf(ios::fixed);
	const int MOD = 998244353;
	int TC = 1, n, m, d;
	cin >> TC;
	while (TC--) {
		cin >> n >> m >> d;
		vector dp(n, vector<int64_t>(m));
		vector g(n, string());
	}
}
