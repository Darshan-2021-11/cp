#pragma GCC optimize("O3,unroll-loops")
#include <bits/stdc++.h>
#include <atcoder/modint>
using namespace std;
using i32 = int;
using u32 = unsigned int;
using i64 = long long;
using u64 = unsigned long long;
using f32 = float;
using f64 = long double;

static const i32 MOD = 998'244'353;
static i32 k, dp[1001], c[26];
int main() {
  ios::sync_with_stdio(false), cin.tie(nullptr), cout.precision(15), cout.setf(ios::fixed);
  cin >> k;
  for (i32 i = 0; i < 26; ++i) cin >> c[i];
  dp[0] = 1;
  for (i32 i = 0; i < k; ++i) {
  }
  set<i32> x;
  x.insert(5);
}
